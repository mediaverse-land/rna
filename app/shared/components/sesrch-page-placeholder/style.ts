import styled from 'styled-components/native';
import { theme } from '../../../constaints/theme';

export const SearchPagePlaceholderComponents = {
    Text: styled.Text`
        line-height: 26.09px;
        color: ${theme.color.light.GRAY};
        font-size: ${theme.fontSize.md};
    `
};
